from pathlib import Path

WORKFLOW_DIR = Path(workflow.basedir)
REPO_ROOT = WORKFLOW_DIR.parent

configfile: str(REPO_ROOT / "config" / "config.yaml")

include: str(WORKFLOW_DIR / "rules" / "manifest.smk")
include: str(WORKFLOW_DIR / "rules" / "alignment.smk")
include: str(WORKFLOW_DIR / "rules" / "qc.smk")
include: str(WORKFLOW_DIR / "rules" / "somatic.smk")
include: str(WORKFLOW_DIR / "rules" / "annotation.smk")
include: str(WORKFLOW_DIR / "rules" / "postprocess.smk")


rule all:
    input:
        str(REPO_ROOT / "results" / "manifest" / "fastq_manifest.tsv"),
        str(REPO_ROOT / "results" / "qc" / "bam_qc_summary.tsv"),
        str(REPO_ROOT / "results" / "tables" / "pass_variants_summary.tsv"),
        str(REPO_ROOT / "results" / "tables" / "vep_variants_summary.tsv")
